<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoFinal.PaginasMenu.Inicio"
             Title="Inicio">

    <Grid>
        <!-- Define las filas y columnas -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Text="Cerrar Sesión"
            HorizontalOptions="End"
            VerticalOptions="Start"
            Clicked="OnLogoutClicked"
            WidthRequest="120"
            Margin="10" />


        <VerticalStackLayout Grid.Row="1" VerticalOptions="Center" HorizontalOptions="Center" Padding="30" Spacing="20">
            <Image Source="logo.png" HeightRequest="250" WidthRequest="250" HorizontalOptions="Center" />

        </VerticalStackLayout>
    </Grid>
</ContentPage>